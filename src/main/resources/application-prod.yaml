# Prod: tắt tài liệu công khai và log SQL.
spring:
  jpa:
    show-sql: false
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
logging:
  level:
    demo.socialanalytics: INFO
    org.hibernate.SQL: WARN
